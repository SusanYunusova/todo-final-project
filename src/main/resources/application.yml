security:
  oauth2:
    client:
      clientId: 579377249444600
      clientSecret: c96125b1b6f8cd6c56d87fb7447968eb
      accessTokenUri: https://graph.facebook.com/oauth/access_token
      userAuthorizationUri: https://www.facebook.com/dialog/oauth
      tokenName: oauth_token
      authenticationScheme: query
      clientAuthenticationScheme: form
    resource:
      userInfoUri: https://graph.facebook.com/me

spring:
  application:
    name: todo
  datasource:
    url: jdbc:mysql://localhost:3306/todo?createDatabaseIfNotExist=true&useUnicode=yes&characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:

      ddl-auto: update
    show-sql: true
  resources:
    chain:
      strategy:
        content:
          enabled: true
          paths: /**
  mail:
    host: smtp.gmail.com
    username: ${app.email}
    password: ${app.password}
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true



server:
  port: ${app.port}

app:
  host: localhost
  port: 9606
  email: todoApp.iba@gmail.com
  password: todoApp123

